# Nexus Launcher — Deep Project Audit Fix

Сделано в этом проходе:
- проверены все Python-файлы через `py_compile`;
- проверены GitHub Actions YAML-файлы;
- исправлена логика проверки обновлений:
  - GitHub Releases теперь основной источник;
  - website `release.json` используется только как fallback;
  - 404 от GitHub Pages больше не выглядит как страшное предупреждение, если GitHub работает;
- усилено скачивание обновления:
  - удаляется старый `.tmp`;
  - увеличен timeout;
  - проверяется, что скачанный файл не пустой;
- сильно оптимизированы PyInstaller spec-файлы:
  - убран `collect_all("PySide6")`;
  - исключены тяжёлые Qt-модули, которые не нужны лаунчеру;
  - это должно уменьшить размер сборки, лог и количество ложных DLL/QML warnings;
- исправлен сайт:
  - roadmap больше не показывает `0.7.4` во всех карточках;
  - добавлены небольшие responsive-fixes;
- добавлен `tools/deep_qa.py`;
- build scripts стали строже и должны падать, если полный релиз не собран.

Что проверено:
- Python syntax: OK;
- workflows YAML: OK;
- файлы для EXE/installer/site присутствуют.

Важно:
- GUI невозможно полноценно открыть в этой Linux-среде без PySide6/Windows, поэтому финальная проверка UI/installer должна быть на твоём Windows.

- версия поднята до `0.7.5`, чтобы автообновление могло увидеть новый релиз.
