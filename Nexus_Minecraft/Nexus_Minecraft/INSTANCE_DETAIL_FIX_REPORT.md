# Nexus Launcher — Instance Details Fix

Исправлено:
- кнопка «Подробнее» снова открывает страницу сборки;
- добавлен отсутствующий метод `create_logs_tab`;
- вкладка «Логи» теперь показывает Minecraft latest.log и последние логи Nexus;
- `create_tabs()` стал безопасным: если одна вкладка упадёт, страница подробностей всё равно откроется, а проблемная вкладка будет показана как предупреждение;
- добавлена функция `file_size_text` для отображения размеров логов;
- сохранены предыдущие улучшения модов, загрузок, тем и адаптивности.

Проверка:
- весь проект проверен через `py_compile`;
- Python-файлов: 61;
- ошибок: 0.
