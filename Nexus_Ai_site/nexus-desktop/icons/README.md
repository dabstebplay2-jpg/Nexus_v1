# Nexus IDE icons

Replace placeholders before release:

- `nexus-ide.ico` — Windows (256×256 multi-size)
- `nexus-ide.icns` — macOS
- `nexus-ide.png` — Linux (512×512)

Copy into VSCodium `icons/` during `prepare` or patch `resources/win32/code.ico` in the vscode tree after build prep.
